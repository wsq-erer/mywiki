这个是mt教的。
                                 									-------
刚去的第一天，mt问的想先干哪一块，有IDS，OTA（车载APP），数据安全，当时说的是IDS非常缺人就去干了，其实连IDS是什么都不知道！

后面才知道IDS分三块，H-IDS，Eth-IDS，Can-IDS，检测的东西不同
环境配置
不敢讲，签保密协议了

H-IDS
主要检测CPU，内存，异常命令，异常行为
CPU / 内存检测
adb shell进入安卓系统，再进入QNX系统，改CPU / 内存相关的文件，将告警阈值降低（测试H-IDS是否正常工作，不是进行攻击），还需要内部的文件，先让告警日志在本地打印，然后发送到VSOC。
T-BOX系统也需要有本地日志，与QNX不一样的就是，可以直接本地打印
异常命令 / 异常行为
个人感觉两者差不多，毕竟执行命令就是行为，命令异常就会导致行为异常，也就触发告警
很多都可以列入异常命令，包括但不限于，远程连接类（ssh，frpc等），查看系统相关信息类（top，mount等），adb类（adb shell，adb pull，adb push等），加解密类（tar），删除（rm）

Eth-IDS
主要检测攻击行为，如各种DOS攻击，ICMP 类型的攻击，拒绝服务攻击，端口扫描
Can-IDS
最陌生的一个测试类型
mt主要教的就是CAN硬件相关的，OBD连接，ZCANpro使用， TSMaster 使用，PCAN使用
ODB连接
教了两种方式古法连接以及OBD盒子，古法连接：杜邦线连接OBD口，下6上14
进行检测的时候需要使用TSMaster进行过29身份认证（也可以直接使用内部诊断仪），才能进行测试
测试
测Can-IDS的时候，ZcanPro比较好用，可以直接发送脚本进行测试，直接看回显就行，

使用PCAN还需要改脚本，而且有时还不准确，ZcanPro进行手工发送报文的时候可以进行连发，Pcan只能手工，有点难受，比如29身份伪造进入扩展会话，每次都要手发10 03看回显是不是50 03  或者7F 10







